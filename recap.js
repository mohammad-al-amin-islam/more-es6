// 1. variable deleration with const and let
// 2. default parameter,5.spread operator
function defaultParameter(array=[]) {
    return Math.max(...array);
}
// 3. Template string 
const names = `name is super khan ${square}`;

// 4.arrow function
const square = x => x * x;